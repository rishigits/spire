template: templates/script-project.yaml
overrides:
  artifacts:
    - type: script
      build_script:
        repo_name: linux-on-ibm-z-scripts
        path: src/build.sh
        args: --version 1.12.4
        docker_image: registry1:5000/ubuntu-docker:22.04
